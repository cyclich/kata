Just playing around with cmake and tdd with a chop kata, using simple iteration and c style arrays

# Get going
Drop googletest into `lib/googletest`

Run `cmake . -B <build dir>`

Build and test

```
cd <build dir>
make
test/chop_test
```
